{
  "name": "assignment3",
  "scripts": {
    "postinstall": "cd angular && npm install && cd ../server && npm install"
  },
  "devDependencies": {
    "chai": "^2.3.0",
    "mocha": "^2.2.4",
    "protractor": "^2.0.0"
  }
}
